import pandas as pd


def feature_scaling(df: pd.DataFrame) -> pd.DataFrame:
    pass


def feature_encoding(df: pd.DataFrame, target_colum: str) -> pd.DataFrame:
    pass


def feature_engineering(df: pd.DataFrame) -> pd.DataFrame:
    pass


# Class imbalance needs addressing at this stage


def dimensionality_reduction(df: pd.DataFrame) -> pd.DataFrame:
    pass


def feature_selection(df: pd.DataFrame) -> pd.DataFrame:
    pass


def split_dataset(df: pd.DataFrame) -> pd.DataFrame:
    pass
