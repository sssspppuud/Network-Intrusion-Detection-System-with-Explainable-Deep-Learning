# Integrating Explainable AI with Deep Learning for Intrusion Detection in IoMT Networks

## Getting Started:
1. Create a python virtual environmnet, and install the required packages using the following:

```cd ./Code```

```python3 -m venv .venv```

```pip install -r requirements.txt```